# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Self-signed dev TLS cert from `npm run dev-secure` (private key — never commit).
certs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

tmp

# Playwright run artifacts (baselines in tests/visual/__screenshots__ ARE committed)
test-results
playwright-report
# Seeded browser state written by tests/visual/seed.setup.ts — regenerated every run
tests/visual/.state
coverage
