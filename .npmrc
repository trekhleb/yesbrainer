# Make the package.json `engines` bound a hard failure, not a warning: an
# `npm install` / `npm ci` on a Node/npm outside the pinned range errors out
# instead of silently producing a divergent tree. See DEVELOPMENT.md → Conventions.
engine-strict=true
